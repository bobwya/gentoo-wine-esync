--- a/0015-server-Create-eventfd-file-descriptors-for-process-o.patch	2018-10-07 03:47:11.000000000 +0100
+++ b/0015-server-Create-eventfd-file-descriptors-for-process-o.patch	2018-11-08 13:09:54.477296422 +0000
@@ -136,9 +136,9 @@
 +    if (do_esync())
 +        process->esync_fd = esync_create_fd( 0, 0 );
 +
-     set_fd_events( process->msg_fd, POLLIN );  /* start listening to events */
-     return process;
- 
+     /* create the main thread */
+     if (pipe( request_pipe ) == -1)
+     {
 @@ -624,6 +630,9 @@ static void process_destroy( struct object *obj )
      if (process->id) free_ptid( process->id );
      if (process->token) release_object( process->token );
