--- a/0007-ntdll-Implement-waiting-on-esync-objects.patch	2018-11-01 17:19:43.000000000 +0000
+++ b/0007-ntdll-Implement-waiting-on-esync-objects.patch	2020-04-29 20:41:36.379346879 +0100
@@ -119,14 +119,14 @@
 +    {
 +        TRACE("Waiting for %s of %d handles:", wait_any ? "any" : "all", count);
 +        for (i = 0; i < count; i++)
-+            DPRINTF(" %p", handles[i]);
++            TRACE(" %p", handles[i]);
 +
 +        if (!timeout)
-+            DPRINTF(", timeout = INFINITE.\n");
++            TRACE(", timeout = INFINITE.\n");
 +        else
 +        {
 +            timeleft = update_timeout( end );
-+            DPRINTF(", timeout = %ld.%07ld sec.\n",
++            TRACE(", timeout = %ld.%07ld sec.\n",
 +                (long) timeleft / TICKSPERSEC, (long) timeleft % TICKSPERSEC);
 +        }
 +    }
