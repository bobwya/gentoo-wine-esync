--- a/0070-ntdll-Store-an-event-s-signaled-state-internally.patch	2018-11-01 17:19:44.000000000 +0000
+++ b/0070-ntdll-Store-an-event-s-signaled-state-internally.patch	2020-04-29 20:41:33.431358457 +0100
@@ -236,7 +236,7 @@
 +}
 +
  /* A value of STATUS_NOT_IMPLEMENTED returned from this function means that we
-  * need to delegate to server_select(). */
+  * need to delegate to server_wait(). */
  NTSTATUS esync_wait_objects( DWORD count, const HANDLE *handles, BOOLEAN wait_any,
 @@ -897,9 +1035,30 @@ NTSTATUS esync_wait_objects( DWORD count, const HANDLE *handles, BOOLEAN wait_an
                      break;
