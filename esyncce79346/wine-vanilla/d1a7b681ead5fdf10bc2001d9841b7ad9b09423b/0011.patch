--- a/0011-ntdll-Implement-NtPulseEvent.patch	2018-11-01 17:19:43.000000000 +0000
+++ b/0011-ntdll-Implement-NtPulseEvent.patch	2020-04-23 17:16:47.991315513 +0100
@@ -56,16 +56,16 @@
 index 2e9142290d..754e9e0d57 100644
 --- a/dlls/ntdll/sync.c
 +++ b/dlls/ntdll/sync.c
-@@ -419,6 +419,9 @@ NTSTATUS WINAPI NtPulseEvent( HANDLE handle, PULONG PulseCount )
+@@ -419,6 +419,9 @@ NTSTATUS WINAPI NtPulseEvent( HANDLE handle, LONG *prev_state )
  {
      NTSTATUS ret;
  
 +    if (do_esync())
 +        return esync_pulse_event( handle );
 +
-     if (PulseCount)
-       FIXME("(%p,%d)\n", handle, *PulseCount);
- 
+     SERVER_START_REQ( event_op )
+     {
+         req->handle = wine_server_obj_handle( handle );
 -- 
 2.19.1
 
